import { useEffect, useMemo, useState } from "react";
import { calculatePpn, parseRupiahToSen, senToRupiah, todayIsoDate } from "@keuangan-apotek/shared";

const apiBase = import.meta.env.VITE_API_URL ?? "http://localhost:3000";
type Account = { id: string; code: string; name: string; classification?: string; normalBalance?: string; level?: number; isGroup?: boolean; isActive?: boolean };
type Api<T> = { data: T } | { error: string | object };
const money = (value: number | string) => { try { const fixed = senToRupiah(typeof value === "number" ? value : parseRupiahToSen(value)).toFixed(2); const [whole, fraction] = fixed.split("."); return `${whole!.replace(/\B(?=(\d{3})+(?!\d))/g, ".")},${fraction}`; } catch { return String(value); } };
const amount = (value: string) => { try { return parseRupiahToSen(value); } catch { return 0; } };
async function request<T>(path: string, init?: RequestInit): Promise<T> { const response = await fetch(`${apiBase}${path}`, init); const body = await response.json() as Api<T>; if (!response.ok || "error" in body) { const error = "error" in body ? body.error : undefined; throw new Error(typeof error === "string" ? error : "Permintaan tidak valid"); } return body.data; }
const inputClass = "w-full rounded-lg border border-slate-200 px-3 py-2 text-sm outline-none focus:border-brand focus:ring-2 focus:ring-blue-100";
const tableClass = "w-full min-w-[980px] text-sm";
function Card({ children }: { children: React.ReactNode }) { return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">{children}</section>; }
function Header({ eyebrow, title, note }: { eyebrow: string; title: string; note: string }) { return <div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-brand">{eyebrow}</p><h2 className="mt-1 text-2xl font-bold tracking-tight">{title}</h2><p className="mt-1 text-sm text-muted">{note}</p></div>; }
function ErrorMessage({ message }: { message: string }) { return message ? <p className="rounded-lg border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-800" role="status">{message}</p> : null; }

type PosRow = { id: string; clearingDate: string; shiftName: string | null; cashierName: string | null; totalPosOmzet: number; physicalCashDiff: number; cogsAmount: number; status: string };
type PosPaymentMethod = { id: string; code: string; name: string; accountId: string; accountCode: string; accountName: string; isCash: boolean; isReceivable: boolean; isActive: boolean; sortOrder: number };
export function POSClearingPage() {
  const [rows, setRows] = useState<PosRow[]>([]);
  const [methods, setMethods] = useState<PosPaymentMethod[]>([]);
  const [message, setMessage] = useState("");
  const [form, setForm] = useState({ clearingDate: todayIsoDate(), shiftName: "", cashierName: "", totalPosOmzet: "", cogsAmount: "", payments: {} as Record<string, string> });
  const activeMethods = methods.filter((method) => method.isActive).sort((a, b) => a.sortOrder - b.sortOrder || a.name.localeCompare(b.name));
  const paymentTotal = activeMethods.reduce((sum, method) => sum + amount(form.payments[method.id] ?? ""), 0);
  const diff = paymentTotal - amount(form.totalPosOmzet);
  const load = async () => {
    try {
      const [posRows, configuredMethods] = await Promise.all([
        request<PosRow[]>("/api/pos-clearings"),
        request<PosPaymentMethod[]>("/api/pos-payment-methods"),
      ]);
      setRows(posRows);
      setMethods(configuredMethods);
      const nextMethods = configuredMethods.filter((method) => method.isActive);
      setForm((current) => ({
        ...current,
        payments: Object.fromEntries(nextMethods.map((method) => [method.id, current.payments[method.id] ?? ""])),
      }));
    } catch (error) {
      setMessage(error instanceof Error ? error.message : "Data POS belum tersedia");
    }
  };

  useEffect(() => { void load(); }, []);

  async function save() {
    if (activeMethods.length === 0) {
      setMessage("Aktifkan minimal satu metode pembayaran POS.");
      return;
    }
    try {
      await request("/api/pos-clearings", {
        method: "POST", headers: { "content-type": "application/json" },
        body: JSON.stringify({
          clearingDate: form.clearingDate, shiftName: form.shiftName, cashierName: form.cashierName,
          totalPosOmzet: form.totalPosOmzet, cogsAmount: form.cogsAmount,
          payments: activeMethods.map((method) => ({ paymentMethodId: method.id, amount: form.payments[method.id] || "0" })),
        }),
      });
      setMessage("Rekap POS tersimpan sebagai draft.");
      setForm((current) => ({ ...current, totalPosOmzet: "", cogsAmount: "", payments: Object.fromEntries(activeMethods.map((method) => [method.id, ""])) }));
      await load();
    } catch (error) { setMessage(error instanceof Error ? error.message : "Rekap belum tersimpan"); }
  }

  async function post(id: string) {
    try { await request("/api/pos-clearings/" + id + "/generate-journal", { method: "POST" }); setMessage("Jurnal POS dan HPP berhasil dibuat."); await load(); }
    catch (error) { setMessage(error instanceof Error ? error.message : "Jurnal belum dibuat"); }
  }

  return <div className="space-y-5">
    <Card>
      <Header eyebrow="Phase 3 · Modul Operasional" title="POS Clearing & HPP Harian" note="Masukkan rekap harian dan nilai HPP. Pengaturan metode pembayaran tersedia di menu Pengaturan." />
      <div className="mt-5 space-y-4">
        <div className="grid gap-3 md:grid-cols-4">
          <label className="text-xs font-semibold text-slate-500">Tanggal<input className={inputClass} type="date" value={form.clearingDate} onChange={(e) => setForm({ ...form, clearingDate: e.target.value })} /></label>
          <label className="text-xs font-semibold text-slate-500">Kasir / shift<input className={inputClass} value={form.cashierName} placeholder="Nama kasir" onChange={(e) => setForm({ ...form, cashierName: e.target.value })} /></label>
          <label className="text-xs font-semibold text-slate-500">Shift<input className={inputClass} value={form.shiftName} placeholder="Shift 1" onChange={(e) => setForm({ ...form, shiftName: e.target.value })} /></label>
          <label className="text-xs font-semibold text-slate-500">Omzet POS<input className={inputClass + " text-right font-mono tabular-nums"} value={form.totalPosOmzet} placeholder="0" onChange={(e) => setForm({ ...form, totalPosOmzet: e.target.value })} /></label>
          {activeMethods.map((method) => <label className="text-xs font-semibold text-slate-500" key={method.id}>{method.name}<span className="mt-1 block text-[10px] font-normal text-slate-400">{method.accountCode} · {method.accountName}</span><input className={inputClass + " text-right font-mono tabular-nums"} value={form.payments[method.id] ?? ""} placeholder="0" onChange={(e) => setForm((current) => ({ ...current, payments: { ...current.payments, [method.id]: e.target.value } }))} /></label>)}
          <label className="text-xs font-semibold text-slate-500">HPP harian<input className={inputClass + " text-right font-mono tabular-nums"} value={form.cogsAmount} placeholder="0" onChange={(e) => setForm({ ...form, cogsAmount: e.target.value })} /></label>
          <div className="flex items-end gap-2"><div className={"flex-1 rounded-lg px-3 py-2 text-sm font-mono tabular-nums " + (diff === 0 ? "bg-emerald-50 text-emerald-700" : "bg-amber-50 text-amber-700")}>Selisih penerimaan: Rp {money(diff)}</div><button className="rounded-lg bg-brand px-4 py-2 text-sm font-semibold text-white" onClick={() => void save()} type="button">Simpan</button></div>
        </div>
        <p className="text-xs text-muted">Metode marketplace dapat diarahkan ke akun piutang; metode bank/QRIS dapat diarahkan ke akun bank atau kliring yang sesuai. Ubah pemetaannya di Pengaturan.</p>
      </div>
    </Card>
    <ErrorMessage message={message} />
    <Card><div className="mb-4 flex items-center justify-between"><div><h3 className="font-semibold">Rekap harian</h3><p className="text-xs text-muted">Selisih penerimaan minus otomatis masuk Beban Selisih Kasir 6106.</p></div></div><div className="overflow-x-auto"><table className={tableClass}><thead className="border-b border-slate-100 text-left text-xs uppercase tracking-wide text-slate-400"><tr><th className="px-3 py-3">Tanggal</th><th className="px-3 py-3">Kasir</th><th className="px-3 py-3 text-right">Omzet</th><th className="px-3 py-3 text-right">Selisih</th><th className="px-3 py-3 text-right">HPP</th><th className="px-3 py-3">Status</th><th className="px-3 py-3" /></tr></thead><tbody>{rows.map((row) => <tr className="border-b border-slate-50" key={row.id}><td className="px-3 py-3 tabular-nums">{row.clearingDate}</td><td className="px-3 py-3">{row.cashierName || row.shiftName || "—"}</td><td className="px-3 py-3 text-right font-mono tabular-nums">Rp {money(row.totalPosOmzet)}</td><td className={"px-3 py-3 text-right font-mono tabular-nums " + (row.physicalCashDiff < 0 ? "text-red-600" : "text-emerald-600")}>Rp {money(row.physicalCashDiff)}</td><td className="px-3 py-3 text-right font-mono tabular-nums">Rp {money(row.cogsAmount)}</td><td className="px-3 py-3"><span className={"rounded-full px-2 py-1 text-xs " + (row.status === "POSTED" ? "bg-emerald-50 text-emerald-700" : "bg-slate-100 text-slate-600")}>{row.status}</span></td><td className="px-3 py-3 text-right"><button className="rounded-lg bg-brand px-3 py-1.5 text-xs font-semibold text-white disabled:opacity-40" disabled={row.status === "POSTED"} onClick={() => void post(row.id)} type="button">Generate Jurnal</button></td></tr>)}</tbody></table></div></Card>
  </div>;
}

type PbfRow = { id: string; invoiceDate: string; dueDate: string; pbfName: string; invoiceNumber: string; dppAmount: number; ppnAmount: number; totalAmount: number; paymentTerms: string; paymentStatus: string };
type PbfDraft = { invoiceDate: string; dueDate: string; pbfName: string; invoiceNumber: string; dppAmount: string; ppnAmount: string; paymentTerms: string };
const emptyPbf = (): PbfDraft => ({ invoiceDate: todayIsoDate(), dueDate: todayIsoDate(), pbfName: "", invoiceNumber: "", dppAmount: "", ppnAmount: "", paymentTerms: "TEMPO_30" });
function pbfDpp(row: PbfDraft) { try { return senToRupiah(parseRupiahToSen(row.dppAmount)); } catch { return senToRupiah(0); } }
export function PBFInvoicesPage() {
  const [rows, setRows] = useState<PbfRow[]>([]); const [drafts, setDrafts] = useState<PbfDraft[]>([emptyPbf()]); const [message, setMessage] = useState("");
  const load = () => request<PbfRow[]>("/api/pbf-invoices").then(setRows).catch((error) => setMessage(error.message)); useEffect(() => { void load(); }, []);
  function update(index: number, patch: Partial<PbfDraft>) { setDrafts((current) => current.map((row, rowIndex) => rowIndex === index ? { ...row, ...patch } : row)); }
  function paste(event: React.ClipboardEvent<HTMLInputElement>, index: number) { const text = event.clipboardData.getData("text"); if (!text.includes("\t") && !text.includes("\n")) return; event.preventDefault(); const pasted = text.trimEnd().split(/\r?\n/).map((line) => line.split("\t")); setDrafts((current) => { const next = [...current]; pasted.forEach((cells, offset) => { const [invoiceDate = todayIsoDate(), dueDate = invoiceDate, pbfName = "", invoiceNumber = "", dppAmount = "", ppnAmount = "", paymentTerms = "TEMPO_30"] = cells.map((value) => value.trim()); next[index + offset] = { invoiceDate, dueDate, pbfName, invoiceNumber, dppAmount, ppnAmount, paymentTerms: paymentTerms || "TEMPO_30" }; }); return next; }); }
  const duplicates = useMemo(() => new Set(drafts.filter((row) => row.pbfName && row.invoiceNumber).map((row) => `${row.pbfName.toLowerCase()}|${row.invoiceNumber.toLowerCase()}`)), [drafts]);
  async function saveAll() { try { for (const row of drafts.filter((item) => item.pbfName && item.invoiceNumber && item.dppAmount)) { const ppn = row.ppnAmount || calculatePpn(pbfDpp(row)).toFixed(2); await request("/api/pbf-invoices", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ ...row, ppnAmount: ppn }) }); } setDrafts([emptyPbf()]); setMessage("Faktur PBF berhasil disimpan dan dijurnal."); await load(); } catch (error) { setMessage(error instanceof Error ? error.message : "Faktur belum tersimpan"); } }
  return <div className="space-y-5"><Card><Header eyebrow="Phase 3 · Modul Operasional" title="Faktur Pembelian PBF" note="Batch ledger dengan Ctrl+V dari Excel, PPN 11% berbasis Decimal, dan jurnal persediaan–utang otomatis." /><div className="mt-5 flex flex-wrap items-center gap-2"><button className="rounded-lg border border-slate-200 px-3 py-2 text-sm font-semibold text-slate-600" onClick={() => setDrafts((current) => [...current, emptyPbf()])} type="button">+ Tambah baris</button><button className="rounded-lg bg-brand px-4 py-2 text-sm font-semibold text-white" onClick={() => void saveAll()} type="button">Simpan & Jurnal Batch</button><span className="text-xs text-muted">Kolom pertama menerima TSV Excel: tanggal, jatuh tempo, PBF, no faktur, DPP, PPN, termin.</span></div></Card><ErrorMessage message={message} /><Card><div className="overflow-x-auto"><table className={tableClass}><thead className="border-b border-slate-100 text-left text-xs uppercase tracking-wide text-slate-400"><tr><th className="px-2 py-3">Tgl faktur</th><th className="px-2 py-3">Jatuh tempo</th><th className="px-2 py-3">PBF</th><th className="px-2 py-3">No. faktur</th><th className="px-2 py-3 text-right">DPP</th><th className="px-2 py-3 text-right">PPN 11%</th><th className="px-2 py-3 text-right">Total</th><th className="px-2 py-3">Termin</th></tr></thead><tbody>{drafts.map((row, index) => { const key = `${row.pbfName.toLowerCase()}|${row.invoiceNumber.toLowerCase()}`; const duplicate = row.pbfName && row.invoiceNumber && drafts.filter((item) => `${item.pbfName.toLowerCase()}|${item.invoiceNumber.toLowerCase()}` === key).length > 1 || rows.some((item) => `${item.pbfName.toLowerCase()}|${item.invoiceNumber.toLowerCase()}` === key); const dpp = pbfDpp(row); const ppn = row.ppnAmount ? pbfDpp({ ...row, dppAmount: row.ppnAmount }) : calculatePpn(dpp); return <tr className="border-b border-slate-50" key={index}><td className="px-2 py-2"><input className={`${inputClass} tabular-nums`} type="date" value={row.invoiceDate} onChange={(e) => update(index, { invoiceDate: e.target.value })} onPaste={(e) => paste(e, index)} /></td><td className="px-2 py-2"><input className={`${inputClass} tabular-nums`} type="date" value={row.dueDate} onChange={(e) => update(index, { dueDate: e.target.value })} /></td><td className="px-2 py-2"><input className={inputClass} value={row.pbfName} placeholder="Kimia Farma" onChange={(e) => update(index, { pbfName: e.target.value })} /></td><td className="px-2 py-2"><input className={`${inputClass} ${duplicate ? "border-red-400 bg-red-50" : ""}`} value={row.invoiceNumber} placeholder="INV-..." onChange={(e) => update(index, { invoiceNumber: e.target.value })} /></td><td className="px-2 py-2"><input className={`${inputClass} text-right font-mono tabular-nums`} value={row.dppAmount} placeholder="0" onChange={(e) => update(index, { dppAmount: e.target.value })} /></td><td className="px-2 py-2"><input className={`${inputClass} text-right font-mono tabular-nums`} value={row.ppnAmount || money(calculatePpn(dpp).times(100).toNumber())} onChange={(e) => update(index, { ppnAmount: e.target.value })} /></td><td className="px-2 py-2 text-right font-mono tabular-nums">Rp {money(`${dpp.plus(ppn).toFixed(2)}`)}</td><td className="px-2 py-2"><select className={inputClass} value={row.paymentTerms} onChange={(e) => update(index, { paymentTerms: e.target.value })}><option value="TUNAI">Tunai</option><option value="TEMPO_14">Tempo 14</option><option value="TEMPO_30">Tempo 30</option><option value="TEMPO_45">Tempo 45</option><option value="TEMPO_60">Tempo 60</option></select></td></tr>; })}</tbody></table></div></Card><Card><h3 className="font-semibold">Faktur tersimpan</h3><div className="mt-3 overflow-x-auto"><table className="w-full min-w-[800px] text-sm"><tbody>{rows.map((row) => <tr className="border-b border-slate-50" key={row.id}><td className="px-3 py-2 tabular-nums">{row.invoiceDate}</td><td className="px-3 py-2">{row.pbfName}</td><td className="px-3 py-2 font-mono">{row.invoiceNumber}</td><td className="px-3 py-2 text-right font-mono tabular-nums">Rp {money(row.totalAmount)}</td><td className="px-3 py-2">{row.paymentStatus}</td></tr>)}</tbody></table></div></Card></div>;
}

type ConsignmentRow = { id: string; vendorId: string; vendorName: string; productName: string; qtySold: number; agreedCostPrice: number; totalPayable: number; status: string };
export function ConsignmentPage() {
  const [rows, setRows] = useState<ConsignmentRow[]>([]); const [accounts, setAccounts] = useState<Account[]>([]); const [selected, setSelected] = useState<string[]>([]); const [message, setMessage] = useState(""); const [vendorId, setVendorId] = useState(""); const [vendorName, setVendorName] = useState(""); const [productName, setProductName] = useState(""); const [qty, setQty] = useState("1"); const [price, setPrice] = useState(""); const [paymentAccountId, setPaymentAccountId] = useState("");
  const load = () => request<ConsignmentRow[]>("/api/consignment/items").then(setRows).catch((error) => setMessage(error.message)); useEffect(() => { void load(); void request<Account[]>("/api/accounts/tree").then((list) => { setAccounts(list.filter((account) => ["1101", "1111", "1112"].includes(account.code))); setPaymentAccountId(list.find((account) => account.code === "1101")?.id ?? ""); }).catch((error) => setMessage(error.message)); }, []);
  const total = rows.filter((row) => selected.includes(row.id)).reduce((sum, row) => sum + row.totalPayable, 0);
  async function add() { try { let id = vendorId; if (!id) { const vendor = await request<{ id: string }>("/api/consignment/vendors", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ vendorName }) }); id = vendor.id; setVendorId(id); } await request("/api/consignment/items", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ vendorId: id, productName, qtySold: Number(qty), agreedCostPrice: price }) }); setMessage("Item konsinyasi ditambahkan."); setProductName(""); setPrice(""); await load(); } catch (error) { setMessage(error instanceof Error ? error.message : "Item belum ditambahkan"); } }
  async function settle() { try { await request("/api/consignment/settlements", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ itemIds: selected, settlementDate: todayIsoDate(), paymentAccountId }) }); setMessage("Tagihan terpilih lunas dan jurnal kas keluar dibuat."); setSelected([]); await load(); } catch (error) { setMessage(error instanceof Error ? error.message : "Pembayaran belum diproses"); } }
  return <div className="space-y-5"><Card><Header eyebrow="Phase 3 · Modul Operasional" title="Konsinyasi & Bagi Hasil" note="Hitung utang vendor dari qty × harga kesepakatan dan selesaikan beberapa item sekaligus." /><div className="mt-5 grid gap-3 md:grid-cols-5"><label className="text-xs font-semibold text-slate-500 md:col-span-2">Nama vendor baru / pilih vendor<input className={inputClass} value={vendorName} placeholder="Vendor herbal" onChange={(e) => setVendorName(e.target.value)} /></label><label className="text-xs font-semibold text-slate-500">Produk<input className={inputClass} value={productName} placeholder="Madu" onChange={(e) => setProductName(e.target.value)} /></label><label className="text-xs font-semibold text-slate-500">Qty<input className={`${inputClass} text-right tabular-nums`} value={qty} onChange={(e) => setQty(e.target.value)} /></label><label className="text-xs font-semibold text-slate-500">Harga bagi hasil<input className={`${inputClass} text-right font-mono tabular-nums`} value={price} placeholder="0" onChange={(e) => setPrice(e.target.value)} /></label></div><button className="mt-3 rounded-lg border border-slate-200 px-4 py-2 text-sm font-semibold text-slate-600" onClick={() => void add()} type="button">+ Tambah item siap bayar</button></Card><ErrorMessage message={message} /><Card><div className="overflow-x-auto"><table className={tableClass}><thead className="border-b border-slate-100 text-left text-xs uppercase tracking-wide text-slate-400"><tr><th className="px-3 py-3" /><th className="px-3 py-3">Vendor</th><th className="px-3 py-3">Produk</th><th className="px-3 py-3 text-right">Qty</th><th className="px-3 py-3 text-right">Harga bagi hasil</th><th className="px-3 py-3 text-right">Total utang</th><th className="px-3 py-3">Status</th></tr></thead><tbody>{rows.map((row) => <tr className="border-b border-slate-50" key={row.id}><td className="px-3 py-3"><input aria-label={`Pilih ${row.productName}`} type="checkbox" checked={selected.includes(row.id)} disabled={row.status === "PAID"} onChange={(e) => setSelected((current) => e.target.checked ? [...current, row.id] : current.filter((id) => id !== row.id))} /></td><td className="px-3 py-3">{row.vendorName}</td><td className="px-3 py-3">{row.productName}</td><td className="px-3 py-3 text-right font-mono tabular-nums">{row.qtySold}</td><td className="px-3 py-3 text-right font-mono tabular-nums">Rp {money(row.agreedCostPrice)}</td><td className="px-3 py-3 text-right font-mono tabular-nums">Rp {money(row.totalPayable)}</td><td className="px-3 py-3"><span className={`rounded-full px-2 py-1 text-xs ${row.status === "PAID" ? "bg-emerald-50 text-emerald-700" : "bg-amber-50 text-amber-700"}`}>{row.status}</span></td></tr>)}</tbody></table></div></Card>{selected.length > 0 && <div className="sticky bottom-4 z-20 flex flex-wrap items-center justify-between gap-3 rounded-2xl border border-blue-200 bg-blue-50 p-4 shadow-lg"><span className="text-sm font-semibold text-blue-900">Selesaikan tagihan terpilih ({selected.length}) — Rp {money(total)}</span><div className="flex items-center gap-2"><select className="rounded-lg border border-blue-200 bg-white px-3 py-2 text-sm" value={paymentAccountId} onChange={(e) => setPaymentAccountId(e.target.value)}>{accounts.map((account) => <option key={account.id} value={account.id}>{account.code} · {account.name}</option>)}</select><button className="rounded-lg bg-brand px-4 py-2 text-sm font-semibold text-white" onClick={() => void settle()} type="button">Bayar & Jurnal</button></div></div>}</div>;
}

type TransferRow = { id: string; transactionTime: string; transactionType: string; sourceAccountId: string; targetAccountId: string; netAmount: number; adminFee: number; totalDeducted: number };
export function CashBankPage() {
  const [accounts, setAccounts] = useState<Account[]>([]); const [summary, setSummary] = useState<Array<Account & { balance: number }>>([]); const [rows, setRows] = useState<TransferRow[]>([]); const [message, setMessage] = useState(""); const [form, setForm] = useState({ transactionTime: `${todayIsoDate()}T09:00`, transactionType: "DEPOSIT", sourceAccountId: "", targetAccountId: "", netAmount: "", adminFee: "", referenceNo: "", memo: "" });
  async function load() { try { const [coa, totals, transfers] = await Promise.all([request<Account[]>("/api/accounts/tree"), request<Array<Account & { balance: number }>>("/api/cash-bank/summary"), request<TransferRow[]>("/api/cash-bank/transfers")]); const cashAccounts = coa.filter((account) => ["1101", "1111", "1112"].includes(account.code)); setAccounts(cashAccounts); setSummary(totals); setRows(transfers); setForm((current) => ({ ...current, sourceAccountId: current.sourceAccountId || cashAccounts.find((account) => account.code === "1101")?.id || "", targetAccountId: current.targetAccountId || cashAccounts.find((account) => account.code === "1111")?.id || "" })); } catch (error) { setMessage(error instanceof Error ? error.message : "Data kas/bank belum tersedia"); } }
  useEffect(() => { void load(); }, []);
  async function save() { try { await request("/api/cash-bank/transfers", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(form) }); setMessage("Mutasi berhasil dicatat dengan jurnal ganda."); setForm({ ...form, netAmount: "", adminFee: "", referenceNo: "", memo: "" }); await load(); } catch (error) { setMessage(error instanceof Error ? error.message : "Mutasi belum tersimpan"); } }
  return <div className="space-y-5"><Card><Header eyebrow="Phase 3 · Modul Operasional" title="Kas, Bank & Mutasi Rekening" note="Setoran kasir, transfer antar rekening, dan biaya admin membentuk jurnal debit-kredit otomatis." /><div className="mt-5 grid gap-4 md:grid-cols-3">{summary.map((account) => <div className="rounded-xl bg-slate-50 p-4" key={account.id}><p className="text-xs text-muted">{account.code} · {account.name}</p><p className="mt-2 font-mono text-xl font-bold tabular-nums">Rp {money(account.balance)}</p></div>)}</div><div className="mt-5 grid gap-3 md:grid-cols-4"><label className="text-xs font-semibold text-slate-500">Waktu<input className={`${inputClass} tabular-nums`} type="datetime-local" value={form.transactionTime} onChange={(e) => setForm({ ...form, transactionTime: e.target.value })} /></label><label className="text-xs font-semibold text-slate-500">Tipe<select className={inputClass} value={form.transactionType} onChange={(e) => setForm({ ...form, transactionType: e.target.value })}><option value="DEPOSIT">Setoran kas ke bank</option><option value="BANK_TRANSFER">Transfer antar bank</option><option value="EXPENSE">Pengeluaran</option><option value="OTHER">Lainnya</option></select></label><label className="text-xs font-semibold text-slate-500">Akun sumber<select className={inputClass} value={form.sourceAccountId} onChange={(e) => setForm({ ...form, sourceAccountId: e.target.value })}>{accounts.map((account) => <option key={account.id} value={account.id}>{account.code} · {account.name}</option>)}</select></label><label className="text-xs font-semibold text-slate-500">Akun tujuan<select className={inputClass} value={form.targetAccountId} onChange={(e) => setForm({ ...form, targetAccountId: e.target.value })}>{accounts.map((account) => <option key={account.id} value={account.id}>{account.code} · {account.name}</option>)}</select></label><label className="text-xs font-semibold text-slate-500">Nominal bersih<input className={`${inputClass} text-right font-mono tabular-nums`} value={form.netAmount} placeholder="0" onChange={(e) => setForm({ ...form, netAmount: e.target.value })} /></label><label className="text-xs font-semibold text-slate-500">Biaya admin<input className={`${inputClass} text-right font-mono tabular-nums`} value={form.adminFee} placeholder="0" onChange={(e) => setForm({ ...form, adminFee: e.target.value })} /></label><label className="text-xs font-semibold text-slate-500">No. referensi<input className={inputClass} value={form.referenceNo} onChange={(e) => setForm({ ...form, referenceNo: e.target.value })} /></label><label className="flex items-end"><button className="w-full rounded-lg bg-brand px-4 py-2 text-sm font-semibold text-white" onClick={() => void save()} type="button">Catat Mutasi</button></label></div></Card><ErrorMessage message={message} /><Card><h3 className="font-semibold">Riwayat mutasi</h3><div className="mt-3 overflow-x-auto"><table className="w-full min-w-[800px] text-sm"><thead className="border-b border-slate-100 text-left text-xs uppercase tracking-wide text-slate-400"><tr><th className="px-3 py-3">Waktu</th><th className="px-3 py-3">Tipe</th><th className="px-3 py-3 text-right">Bersih</th><th className="px-3 py-3 text-right">Admin</th><th className="px-3 py-3 text-right">Total sumber</th></tr></thead><tbody>{rows.map((row) => <tr className="border-b border-slate-50" key={row.id}><td className="px-3 py-3 tabular-nums">{row.transactionTime}</td><td className="px-3 py-3">{row.transactionType}</td><td className="px-3 py-3 text-right font-mono tabular-nums">Rp {money(row.netAmount)}</td><td className="px-3 py-3 text-right font-mono tabular-nums">Rp {money(row.adminFee)}</td><td className="px-3 py-3 text-right font-mono tabular-nums">Rp {money(row.totalDeducted)}</td></tr>)}</tbody></table></div></Card></div>;
}
